{
  "name": "@labos/hpc-bridge",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "labos-hpc-bridge": "dist/cli.js"
  },
  "main": "dist/index.js",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "tsup src/cli.ts src/index.ts --format esm --clean",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm -w --filter @labos/hpc-bridge... build && node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@labos/cli-utils": "workspace:*",
    "@labos/protocol": "workspace:*",
    "uuid": "^11.1.0",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/node": "^22.18.1",
    "@types/ws": "^8.18.1",
    "tsup": "^8.5.0",
    "typescript": "^5.9.2"
  }
}
