{
  "name": "@epoch/codex-spec",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "files": [
    "schema/",
    "metadata.json"
  ],
  "scripts": {
    "refresh": "rm -rf schema/ts schema/json && mkdir -p schema/ts schema/json && codex app-server generate-ts --out schema/ts && codex app-server generate-json-schema --out schema/json && node ./scripts/write-metadata.mjs",
    "check": "node ./scripts/check-generated.mjs"
  }
}
